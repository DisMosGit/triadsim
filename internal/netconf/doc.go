// Package netconf implements the NETCONF management plane: the SSH subsystem,
// hello and capabilities, EOM and chunked framing, RPC parsing and dispatch,
// the running/candidate/startup operations, confirmed-commit and
// create-subscription notifications.
package netconf
